# ST-GNN ??????

## 1) ???????
```bash
python data_quality.py --input final_real_data.csv --clean-output final_real_data_clean.csv --report-json artifacts/data_quality_report.json --min-user-inter 5 --min-item-inter 2
python data_quality.py --input final_real_data.csv --clean-output final_real_data_clean_strict.csv --report-json artifacts/data_quality_report_strict.json --min-user-inter 10 --min-item-inter 10
```

## 2) ????
```bash
python train_stgnn.py --db rec_system.db --artifact artifacts/stgnn_artifact_v2.pt --epochs 6 --embed_dim 64
```

## 3) ?????? + ?? + ??????
```bash
python experiment_suite.py --input final_real_data_clean_strict.csv --output-dir artifacts/experiments --sample-users 80 --topk 10 --stgnn-epochs 4 --lightgcn-epochs 2
```

????????
```bash
python experiment_suite.py --input final_real_data_clean_strict.csv --output-dir artifacts/experiments --sample-users 40 --topk 10 --stgnn-epochs 2 --lightgcn-epochs 1 --skip-sensitivity
```

## 4) Web ?? App?Streamlit?
```bash
streamlit run app.py
```

?????
- ??????????? + ????
- ??????????????????????????

## 5) API ??????? App / ??? ???
```bash
uvicorn api_server:app --host 0.0.0.0 --port 8000
```

???
- `GET /health`
- `GET /recommend/{user_id}?top_k=10`
- `POST /interact`  
  body: `{ "user_id": 1, "item_id": 12345, "behavior": 0, "timestamp": 1710000000 }`

## 6) ?????? App ?????
1. ??????????????
2. ? Flutter / uni-app / React Native ??????
3. ??????? API ??????????
4. ???????????? artifact?
